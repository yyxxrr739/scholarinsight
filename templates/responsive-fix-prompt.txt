你是一个专业的HTML响应式设计优化专家。请根据提供的CSS模板@responsive-report-template.css ，将以下学术报告HTML文件.@Active_Inference_Institute_Analysis_Report.html  优化为移动端友好的版本，同时注意更新元数据 @reports.json 。
## 优化要求：

1. **必须添加viewport元标签**（如果缺失）：
```html
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
```

2. **应用完整的响应式CSS样式**：
- 使用提供的CSS模板内容替换或补充现有的<style>标签内容
- 确保包含完整的移动端媒体查询 (@media only screen and (max-width: 768px))

3. **关键优化点**：
- 所有容器添加 overflow-x: hidden
- 文本元素添加 word-wrap: break-word 和 overflow-wrap: break-word
- 链接添加 word-break: break-all
- 表格添加横向滚动支持
- 图片设置 max-width: 100%
- 图表容器添加响应式处理

4. **保持原有功能**：
- 不改变HTML结构
- 保留所有原有的class和id
- 保持原有的JavaScript功能
- 不修改报告内容

## 处理步骤：

1. 检查HTML文件是否包含viewport元标签，如缺失则添加
2. 找到<style>标签，将其内容替换为完整的响应式CSS模板
3. 如果原文件有多个<style>标签，合并为一个
4. 确保CSS语法正确，没有遗漏的括号或分号
5. 验证移动端媒体查询包含所有必要的样式规则

## 输出要求：

- 返回完整的优化后的HTML文件
- 确保代码格式正确，缩进一致
- 添加注释说明关键优化点（可选）

请现在开始优化提供的HTML报告文件。