# ScholarInsight 阿里云ECS部署规划

## 📌 部署目标

将ScholarInsight网站部署到阿里云ECS服务器，通过Nginx提供Web服务。

## 🎯 部署信息

### 服务器信息
- **服务器IP**: `118.31.238.102`
- **SSH用户**: `root`
- **SSH密钥路径**: `~/.ssh/key_scholarinsight.pem`
- **部署路径**: `/usr/share/nginx/html/`
- **Web服务器**: Nginx

### 域名信息
- 域名已购买并完成ICP备案
- 需要在DNS配置A记录指向服务器IP

## 📋 部署流程概览

### 阶段一：准备工作
1. ✅ 检查本地环境（Node.js、npm）
2. ✅ 检查SSH密钥文件
3. ✅ 确认项目代码最新
4. ⚠️ **需要确认**：Next.js配置是否支持静态导出

### 阶段二：本地构建
1. 安装依赖（如需要）
2. 执行 `npm run build` 构建静态文件
3. 验证 `out/` 目录生成

### 阶段三：服务器准备
1. SSH连接测试
2. 检查/安装Nginx
3. 创建/备份部署目录
4. 配置Nginx（需要提供域名）

### 阶段四：文件上传
1. 上传 `out/` 目录内容
2. 上传 `public/` 目录内容（如需要）
3. 设置文件权限

### 阶段五：Nginx配置
1. 创建Nginx配置文件
2. 配置域名（需要提供实际域名）
3. 配置静态资源缓存
4. 配置路由规则

### 阶段六：DNS配置
1. 配置A记录指向服务器IP
2. 等待DNS生效

### 阶段七：验证测试
1. 通过IP访问测试
2. 通过域名访问测试
3. 检查所有功能页面
4. 检查静态资源加载

## ⚠️ 需要确认的事项

### 1. Next.js静态导出配置
当前 `next.config.js` 未明确配置 `output: 'export'`，但项目已有 `out/` 目录。

**建议**：确认是否需要添加以下配置：
```javascript
const nextConfig = {
  output: 'export',  // 启用静态导出
  images: {
    domains: ['picture-search.tiangong.cn'],
  },
}
```

### 2. 域名信息
部署文档中使用占位符 `your-domain.com`，需要提供：
- 实际域名
- 是否需要配置www子域名

### 3. SSL证书
- 是否需要配置HTTPS？
- 建议使用Let's Encrypt免费证书

### 4. 部署方式选择
- **方式一**：使用自动化脚本 `deploy.sh`（推荐）
- **方式二**：手动执行部署步骤

## 📁 已创建的文档和脚本

1. **ECS_DEPLOYMENT.md** - 详细部署文档
   - 包含完整的部署步骤
   - 故障排除指南
   - 安全建议
   - 性能优化建议

2. **deploy.sh** - 自动化部署脚本
   - 自动构建项目
   - 自动上传文件
   - 自动配置权限
   - 自动重载Nginx

## 🚀 部署执行计划

### 如果使用自动化脚本：
```bash
# 在项目根目录执行
./deploy.sh
```

### 如果手动部署：
按照 `ECS_DEPLOYMENT.md` 文档逐步执行

## 📝 部署前检查清单

- [ ] 确认域名信息
- [ ] 确认Next.js配置是否需要更新
- [ ] 确认SSH密钥文件存在且权限正确
- [ ] 确认ECS服务器运行正常
- [ ] 确认DNS解析配置
- [ ] 确认是否需要配置SSL证书

## 🔄 后续维护

### 更新部署
当需要更新网站时：
1. 本地执行 `npm run build`
2. 运行 `./deploy.sh` 或手动上传文件
3. 自动备份旧版本

### 监控建议
- 定期检查Nginx日志
- 监控服务器资源使用
- 定期备份网站文件

## 📞 下一步

**请确认以下信息后，我将执行部署操作：**

1. ✅ 域名名称（用于Nginx配置）
2. ✅ 是否需要配置HTTPS
3. ✅ 是否使用自动化脚本部署
4. ✅ Next.js配置是否需要添加 `output: 'export'`

确认后，我将：
1. 更新Next.js配置（如需要）
2. 更新Nginx配置文件中的域名
3. 执行部署操作
4. 验证部署结果

